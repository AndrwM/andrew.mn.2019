nav.c-navigation
  .c-container
    a.c-link.c-link--arrow-l(href="/about/#🚀") Andrew McIntee
    if breadcrumb
      each link, index in breadcrumb
        | &nbsp;/&nbsp;
        if link.active
          span.c-link.c-link--active #{link.name}
        else
          a.c-link.c-link--arrow-l(href="/about/#{link.path}/#🚀") #{link.name}
